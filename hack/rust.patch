diff --git a/rust/Cargo.toml b/rust/Cargo.toml
index 161470a..317d3e0 100644
--- a/rust/Cargo.toml
+++ b/rust/Cargo.toml
@@ -2,14 +2,12 @@
 name = "chris-oag"
 version = "0.0.1"
 authors = ["dev@babymri.org"]
-description = "The ChRIS Ultron BackEnd (CUBE) is the core backend API of ChRIS. It manages ChRIS users, plugins, pipelines, and the provenance of data analyses as ChRIS feeds."
+description = "OpenAPI models for the ChRIS backend, generated by OpenAPI-generator."
 license = "MIT"
-edition = "2021"
+edition = "2024"
 
 [dependencies]
 serde = { version = "^1.0", features = ["derive"] }
 serde_with = { version = "^3.8", default-features = false, features = ["base64", "std", "macros"] }
 serde_json = "^1.0"
-serde_repr = "^0.1"
-url = "^2.5"
-reqwest = { version = "^0.12", default-features = false, features = ["json", "multipart"] }
+uuid = { version = "1.17.0", features = ["serde"] }
diff --git a/rust/src/models/plugin_instance_request.rs b/rust/src/models/plugin_instance_request.rs
index 5ae443b..d6c7a47 100644
--- a/rust/src/models/plugin_instance_request.rs
+++ b/rust/src/models/plugin_instance_request.rs
@@ -12,7 +12,7 @@ use crate::models;
 use serde::{Deserialize, Serialize};
 
 #[derive(Clone, Default, Debug, PartialEq, Serialize, Deserialize)]
-pub struct PluginInstanceRequest {
+pub struct PluginInstanceRequest<T> {
     #[serde(rename = "title", skip_serializing_if = "Option::is_none")]
     pub title: Option<String>,
     #[serde(rename = "compute_resource_name", skip_serializing_if = "Option::is_none")]
@@ -27,10 +27,14 @@ pub struct PluginInstanceRequest {
     pub gpu_limit: Option<i32>,
     #[serde(rename = "previous_id", skip_serializing_if = "Option::is_none")]
     pub previous_id: Option<i32>,
+
+    /// Plugin parameters
+    #[serde(flatten)]
+    pub parameters: T,
 }
 
-impl PluginInstanceRequest {
-    pub fn new() -> PluginInstanceRequest {
+impl<T> PluginInstanceRequest<T> {
+    pub fn new(parameters: T) -> Self {
         PluginInstanceRequest {
             title: None,
             compute_resource_name: None,
@@ -39,6 +43,7 @@ impl PluginInstanceRequest {
             number_of_workers: None,
             gpu_limit: None,
             previous_id: None,
+            parameters,
         }
     }
 }
